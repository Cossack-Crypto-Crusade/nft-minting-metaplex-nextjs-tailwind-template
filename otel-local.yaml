receivers:
  otlp:
    protocols:
      http:

exporters:
  otlphttp/tempo:
    endpoint: "http://localhost:3200"
  logging:
    loglevel: debug

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp/tempo, logging]
